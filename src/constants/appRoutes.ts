export const appRoutes = {
  home: '/',
  projectDetails: '/project/:id',

  // Reserved future routes (NOT used yet).
  blogs: '/blogs',
  blogDetails: '/blogs/:id',
  articles: '/articles',
  articleDetails: '/articles/:id',
  admin: '/admin',
} as const;

